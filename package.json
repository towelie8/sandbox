{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "sandbox. tryout",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss -i ./src/styles/styles.css -o ./src/styles/output.css",
    "dev": "live-server ./src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.4.9"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
